## Accordion:
1. read the input data from data.ts
   
2. iterate and render the data with modular components.
 
4. install react icons to create toggle buttons on condition.
 
5. create a state value (boolean) to toggle the display of  info.
 
6. toggle the info on basis of button click which toggles the state value.

--- 

## Accordion (Radio):

1. read the input data from data.ts
   
2. iterate and render the data with modular components.
   
3. install react icons to create toggle buttons on condition.
   
4. create a state value to store the active Id.
   
5. create a function for assigning Id of Question to the state value.
   
6. on button click, assign the state value with the Id of the question item. Ensure if active Id === Id, then display the info that matches this condition. 
  
7. Handle the Info closing logic inside the button click function. If Id is not matching return null else the matching Id.  
